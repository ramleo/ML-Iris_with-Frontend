# ML Pipeline Summary

_Generated by auto_pipeline.py on 2026-05-28 00:25:51_

---

## 1. Dataset Overview

| Property | Value |
|---|---|
| File | Iris.csv |
| Rows | 150 |
| Columns | 6 |
| Target Column | `Species` |
| Task Type | **Classification** |
| Unique Target Values | 3 |
| Deployment Platform | render |

### Missing Values
No missing values detected.

---

## 2. Features Used

**Numeric (5):** `Id`, `SepalLengthCm`, `SepalWidthCm`, `PetalLengthCm`, `PetalWidthCm`

**Categorical (0):** _none_

**Dropped (>50% missing):** _none_

---

## 3. Preprocessing Pipeline

| Step | Detail |
|---|---|
| Missing (numeric) | SimpleImputer(strategy="median") |
| Missing (categorical) | SimpleImputer(strategy="most_frequent") |
| Scaling | StandardScaler |
| Encoding | OneHotEncoder(handle_unknown="ignore") |
| Train/Test Split | 80/20, stratified, random_state=42 |
| Train size | 120 rows |
| Test size | 30 rows |

---

## 4. Model Selection & Hyperparameter Tuning

GridSearchCV(cv=3, n_jobs=-1, scoring="accuracy")

| Model | CV Score | Best Params |
|---|---|---|
| LogisticRegression | 1.0000 | {'C': 10} |
| RandomForest | 1.0000 | {'max_depth': None, 'n_estimators': 100} |
| GradientBoosting | 0.9833 | {'learning_rate': 0.05, 'n_estimators': 100} |

**Winner:** `LogisticRegression` with CV accuracy = **1.0000**

---

## 5. Model Evaluation

| Metric | Value |
|---|---|
| Test Accuracy | 1.0000 |
| CV Score (accuracy) | 1.0000 |

**Classification Report:**
```
                 precision    recall  f1-score   support

    Iris-setosa       1.00      1.00      1.00        10
Iris-versicolor       1.00      1.00      1.00        10
 Iris-virginica       1.00      1.00      1.00        10

       accuracy                           1.00        30
      macro avg       1.00      1.00      1.00        30
   weighted avg       1.00      1.00      1.00        30

```

---

## 6. Final Pipeline Architecture

```
CSV Input
    └── ColumnTransformer
            ├── numeric  → SimpleImputer(median) → StandardScaler
            └── categorical → SimpleImputer(most_frequent) → OneHotEncoder
                └── LogisticRegression(C=10)
                        └── Prediction
```

---

## 7. Artifacts

| File | Description |
|---|---|
| models/final_pipeline.pkl | Trained sklearn Pipeline (preprocessor + model) |
| models/label_encoder.pkl | Fitted LabelEncoder for target classes |
| plots/eda_correlation.png | Feature correlation heatmap |
| plots/eda_target.png | Target variable distribution |


---

## 8. Reproducibility

```python
import joblib, pandas as pd

pipeline = joblib.load("models/final_pipeline.pkl")
df = pd.read_csv("data/Iris.csv")
X = df.drop(columns=["Species"])
predictions = pipeline.predict(X)
print(predictions)
```
